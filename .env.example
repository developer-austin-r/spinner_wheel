# Copy this file to .env before deploying and replace the example secrets.
APP_PORT=8080
POSTGRES_DB=spinner_wheel
POSTGRES_USER=spinner
POSTGRES_PASSWORD=replace-with-a-strong-database-password
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
SPINNER_LIMIT=10
